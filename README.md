
  # University Admission Management System (Copy)

  This is a code bundle for University Admission Management System.
  
  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.

  ## Dynamic Template Form Feature

  Full documentation for template upload, field mapping, dynamic student form rendering, and PDF generation is available at:

  - `docs/template-form-feature.md`

  ## Dual Database (PostgreSQL + MongoDB)

  Dual-database architecture and setup guide:

  - `docs/dual-database-architecture.md`
  
